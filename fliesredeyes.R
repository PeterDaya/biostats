setwd("~/Desktop/NYU/Biostatistics/week_4")
pbinom( 30 , 32 , .5 , lower.tail = FALSE ) #This command finds the probability of a randomly chosen group having 30 or more red eyed alleles.  lower.tail=FALSE so that it finds the probability for values greater than 30.
